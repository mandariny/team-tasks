# Architecture — Team-Tasks MVP

```
              Browser
                 │
  Vercel : Next.js (Front + API Routes)
          │                    │
   Google OAuth      Supabase (Postgres + Auth)
```
